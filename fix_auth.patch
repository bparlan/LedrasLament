--- a/fal_generate.py
+++ b/fal_generate.py
@@ -158,7 +158,14 @@
 
             print(f"🤖 Calling fal.run() API...")
             result = fal_run(self.config.fal_model, arguments=fal_params)
+            # Add API key authentication for fal.run()
+            api_key = os.environ.get('FAL_API_KEY')
+            if api_key:
+                print(f"🔐 Using fal.ai API key for authentication")
+                result = fal_run(self.config.fal_model, arguments=fal_params, headers={"Authorization": f"Bearer {api_key}"})
+
+# Result may be a dict with an "images" list, each entry either a URL dict or a base64 string.